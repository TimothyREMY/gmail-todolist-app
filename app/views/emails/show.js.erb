alert("le fichier JS est exécuté!");

  document.getElementById('<%= @email.id %>').classList.add("bg-success")

document.getElementById("body_email").innerHTML="<%= j render 'show', email: @email%>";
